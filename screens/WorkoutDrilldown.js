import { StyleSheet, View, SafeAreaView, FlatList } from 'react-native';
import { Text, Button, TextInput } from 'react-native-paper';

function WorkoutDrilldown({}) {
    return (<View></View>);
}

export default WorkoutDrilldown;